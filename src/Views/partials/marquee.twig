<div class="marquee-strip">
    <div class="marquee-inner" id="marqueeInner">
        {% for tech in profile.marquee_tech %}
            <span>{{ tech }}</span><span class="dot">&middot;</span>
        {% endfor %}
        {% for tech in profile.marquee_tech %}
            <span>{{ tech }}</span><span class="dot">&middot;</span>
        {% endfor %}
    </div>
</div>
